{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node ./scripts/preinstall.mjs",
    "build": "pnpm run typecheck && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/docpocket run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm --filter @workspace/api-server run typecheck && pnpm --filter @workspace/docpocket run typecheck && pnpm --filter @workspace/scripts run typecheck",
    "dev:docpocket": "pnpm --filter @workspace/docpocket run dev",
    "dev:api-server": "pnpm --filter @workspace/api-server run dev",
    "build:docpocket:apk": "pnpm --filter @workspace/docpocket run apk",
    "build:api-server": "pnpm --filter @workspace/api-server run build"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
